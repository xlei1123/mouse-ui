import { defineConfig } from 'dumi';

export default defineConfig({
  title: 'reactUI',
  // favicon: './TNTWEB.png',
  logo: './logo.png',
  outputPath: 'docs',
  publicPath: './',
  // more config: https://d.umijs.org/config
});
