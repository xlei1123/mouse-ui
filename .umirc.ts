import { defineConfig } from 'dumi';

export default defineConfig({
  title: 'reactUI',
  // favicon: './TNTWEB.png',
  // logo: './TNTWEB.png',
  outputPath: 'docs',
  publicPath: './',
  // more config: https://d.umijs.org/config
});
